@page
@model CS392_WebApplication.Pages.SchoolUserPages.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>School_User</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="School_User.userID" />
            <div class="form-group">
                <label asp-for="School_User.school_name" class="control-label"></label>
                <input asp-for="School_User.school_name" class="form-control" />
                <span asp-validation-for="School_User.school_name" class="text-danger"></span>
            </div>
            <div class="form-group form-check">
                <label class="form-check-label">
                    <input class="form-check-input" asp-for="School_User.is_approved" /> @Html.DisplayNameFor(model => model.School_User.is_approved)
                </label>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
